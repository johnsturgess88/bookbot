def get_num_words(text):
        return len(text.split())

def count_characters(text):
        counts = {}
        for ch in text:
            ch = ch.lower()
            counts[ch] = counts.get(ch, 0) +1
        return counts
        
def sort(text):
       return counts.sort(reverse = true, key=sort)
       